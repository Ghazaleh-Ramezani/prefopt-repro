_base_: base.yaml
name: sft
stage: sft
train:
  output_dir: runs/sft
  learning_rate: 1.0e-5
  num_train_epochs: 1.0
  per_device_train_batch_size: 4
  gradient_accumulation_steps: 8
method:
  packing: true
notes: >
  Baseline for every win-rate in this repo. Deliberately plain: one epoch,
  cosine schedule, no data curation beyond the split.
